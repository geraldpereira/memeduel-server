package fr.byob.game.memeduel.server.rest;

import com.google.inject.AbstractModule;

public class DozerModule extends AbstractModule{

	@Override
	protected void configure() {
		// final DozerBeanMapper mapper = new
		// DozerBeanMapper(Arrays.asList("dozer-mapping.xml"));
		// bind(Mapper.class).toInstance(mapper);
	}

}
